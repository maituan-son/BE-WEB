const express = require("express");
const router = express.Router();
const CartitemController = require("./cartitem.controller");

// Define routes here
// Example: router.get('/', CartitemController.getAll);

module.exports = router;
