const express = require("express");
const router = express.Router();
const OrderController = require("./order.controller");

// Define routes here
// Example: router.get('/', OrderController.getAll);

module.exports = router;
