const express = require("express");
const router = express.Router();
const OrderitemController = require("./orderitem.controller");

// Define routes here
// Example: router.get('/', OrderitemController.getAll);

module.exports = router;
