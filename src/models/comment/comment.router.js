const express = require("express");
const router = express.Router();
const CommentController = require("./comment.controller");

// Define routes here
// Example: router.get('/', CommentController.getAll);

module.exports = router;
